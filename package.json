{
  "name": "popokdegen",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "preview:static": "python3 -m http.server 8788 --directory public",
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name popokdegen"
  },
  "dependencies": {
  "@cloudflare/pages-plugin-vercel-og": "^0.1.2",
  "react": "^18.3.1"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
